<div class="container-fluid pages <%= @post.the_slug %>">
    <div class="main-bg" style="background-image:url(<%= raw @post.the_thumb_url() if @post.has_thumb?%>);">


            <h1 class="post-title"><%= @post.the_title %></h1>
            <div class="page-content"><%= raw @post.the_content %></div>
    </div>
    
</div>

